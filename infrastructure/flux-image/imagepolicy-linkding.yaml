apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy

metadata:
  name: linkding
  namespace: flux-system

spec:
  imageRepositoryRef:
    name: linkding

  policy:
    semver:
      range: ">=1.45.0"
